This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

- Install [Node.js](https://nodejs.org/en/download/package-manager)
- Go to project home directory and run the command

```bash
npm install
```

- Build and run the server. Run the command

```bash
npm run build
```

after build is finished, run

```bash
npm run start
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## To Test

- Fill out the Referral form and click **CREATE REFERRAL** button
- The preview should be updated automatically
